# AGENTS.md

## 저장소 목적

이 저장소는 Codex를 주 실행 환경으로 사용하는 개인용 에이전트 스킬 모음이다. 사용자 문서와 대화는 한국어를 기본으로 한다.

## 스킬 구조

- 스킬은 `skills/<kebab-case-name>/`에 둔다.
- 각 스킬은 `SKILL.md`를 반드시 포함한다.
- 사용자용 설명은 필요할 때 `README.md`에 작성한다.
- 재사용 코드, 예시 이미지, 참고 자료는 각각 `scripts/`, `assets/`, `references/`에 둔다.
- 스킬에 불필요한 의존성, 글꼴, 생성 중간 파일을 포함하지 않는다.

## 작성 원칙

- `SKILL.md`는 에이전트가 따를 수 있도록 명확하고 간결하게 작성한다.
- 스킬명, 명령어, 경로, 코드 식별자는 영어를 사용한다.
- 사용자가 받는 설명과 예시는 한국어로 작성한다.
- 새 스킬은 언제 사용해야 하는지 `description`에 분명히 적는다.
- 외부 도구나 라이브러리가 필요하면 설치 방법과 검증 방법을 함께 적는다.

## 변경과 검증

- 기존 스킬의 동작을 바꾸기 전 관련 `SKILL.md`와 README를 확인한다.
- 이미지, 문서, 스크립트 산출물은 실제로 열거나 실행해 검증한다.
- 생성 과정에서 만든 임시 파일과 의존성 폴더는 커밋하지 않는다.

## Git

- 커밋 메시지는 `commit-message` 스킬의 규칙을 따른다.
- 요청 범위 밖의 변경은 스테이징하거나 커밋하지 않는다.
- 이 저장소는 공개 GitHub 레포지토리다. 스테이징하거나 커밋하기 전, 대상 파일에 사용자명, 계정·연락처 정보, API 키·토큰·비밀번호, 로컬 절대 경로, 내부 시스템 정보 등 민감 정보가 포함되어 있는지 검토한다.
- 민감 정보 또는 공개에 부적절한 정보가 발견되면 스테이징·커밋하지 않고, 발견 위치와 위험을 사용자에게 설명한 뒤 처리 방향을 확인한다.
- amend, reset, rebase, push는 명시적인 요청이 있을 때만 수행한다.
